要求：实现 添加 删除 修改 之后页面不能刷新
    基本功能：
    1.增加 修改 查询 删除  其中“查询”不代表“搜索”，只要页面刷新后，数据能够正常显示即可。
    2.可以储存 “内容” 与 “是否已完成” 的状态
    3.使用 localStorage 储存数据
    4.优化界面
    可以只显示“未完成”或“已完成”的条目
        你可以把它们分开显示，或者是通过按钮切换显示，或者你也可以自己想一种把它们分开显示的方法
    给每一条 todo 添加 “完成日期”
    在第二点的基础上，已过期的条目有不同的样式
    其它更牛逼的功能

思路：
    一.创建界面
        界面分为header container footer
        
    二.各项目的实现方法(不刷新原始界面)
        1.文本输入框，提交按钮
            输入文本框(input).
        2.添加功能
        3.查询功能
        4.修改功能
        5.默认未完成，选择后已完成。默认在左侧，完成后在右侧
        6.完成时添加完成日期
        7.优化界面
        

    二.todoList 实现
        1.先获取本地库，localStorage.getItem
        2.判断输入的值是哪一个 null Json 
        3.存在 将Json转换成数组 JSON.parse(); 没有数据，初始化数组
        4.添加todo  之后清空  根据数组渲染页面 render(){}  判断状态添加到对应的阵营  写模板









